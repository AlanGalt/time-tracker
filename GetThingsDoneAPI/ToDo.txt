4) Frontend;
5) Documentation and comments if time is left.

"Пользователь должен иметь возможность видеть список всех проводок за все время"
	- для корректной реализации нужно делать частичную подгрузку.

Можно было разделить функционал общения с бд и бизнес логики (валидация),
но для небольшо проекта, думаю, усложнять не стоит.

В более крупном проекте, конечно, это должны быть две разных сущности, 
каждая со своей единственной ответственностью.

Можно добавить асинхроность.

Скрывать perfomance за месяц. Отображать стикеры только там, где есть проводки.
Идти по массиву проводок и добавлять часы к соотв. дням в объекте.

Если задача стала неактивна после заведения проводки, 
запретить редактирования поля "Задача" для данной проводки:
	реализуется на фронте. 
	для каждой проводки запрашиваешь задачу по id, 
	смотришь статус и отключаешь редактирование, если задача неактивна.